name: Simple Profile Update

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Update last updated timestamp
        uses: gautamkrishnar/keepalive-workflow@master
